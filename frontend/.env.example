# Rename this file to .env for local overrides (not required for local dev,
# since vite.config.js already proxies /api to http://localhost:5000).
#
# In production (Vercel), set this to your deployed Render backend URL,
# e.g. https://creditlens-backend.onrender.com  (no trailing slash)
VITE_API_URL=
